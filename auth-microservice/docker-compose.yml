version: '3.8'
services:
  auth:
    container_name: auth
    build: .
    ports:
      - '3001:3001'
    env_file:
      - .env
    networks:
      - kafka_network

networks:
  kafka_network:
    driver: bridge
